# Copy to /var/www/app/sibooking/frontend/.env.production on the VPS.
# Frontend values are exposed to the browser when prefixed with NEXT_PUBLIC_.
# Do not put backend secrets, database URLs, or private tokens here.

NEXT_PUBLIC_API_BASE_URL=/api
