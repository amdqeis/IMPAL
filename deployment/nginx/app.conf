# Reference only. setup.sh generates the active Nginx site at:
# /etc/nginx/sites-available/sibooking
#
# The generated site proxies:
# - /api/ to unix:/run/sibooking/backend.sock
# - / to unix:/run/sibooking/frontend.sock
